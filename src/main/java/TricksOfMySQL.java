public class TricksOfMySQL {
    /*
    -->SQL(Structured Query Language) yapılandırılmış sorgu dilidir. (Programlama dili değil sorgu dilidir)
    -->Excel bir data basedir.
    -->Veri tabanı genellikle bir Veritabanı Yönetim Sistemi DBMS (DataBaseManagementSystem) ile kontrol edilri
    -->SQL en güçlü yanı hızlı raporlama yapması
    -->Sarıtlar yani Row'lar objeye karşılık gelir. Her bir hücreyede record adı verilir.
    -->Tablolarda class olarak benzetilebilir.
    -->Or'un olduğu yerde In olması için column objective olmalıdır. In de aynı olmalı orda farklı olmalı maas ve isim de or sadece maaslarda in kullanılabilir
    -->Veri tabanı yapılandırılmış bir şekilde organize edilmiş bilgi depolama alanıdır.
    -->Tabloyu class sütunları field recordlarıda objeye benzetebiliriz.
    -->ERD entity relation diagram demektir.
    -->SQL roporladama hızlı erişimde zayıftır. NOSQL de ise erişimde hızlı raporlamada yavaş
    -->Data base projeye chema page'a tabloda class'a karşılık gelir. (Anlam olarak)
    -->Char tanımlanan kadar yer ayırır varchar girdiğin kadar yer ayırır. varchar (50) burda üst sınırı verir ama char(50) sadece 50 karakter içermeli ne az ne çok
    -->SQL de best practice olarak tek satır da yazmaktır create table kısmında
    -->Var olan tablodan tablo oluşturma
    var olan tablo create table( id char(11),name carchar(50), grade int, address varchar(100),last_update date);
    yeni tablo ise create table student_grade as select name,grade from student_table;
    -->^ bu başlama $ bu ifade ise bitimi verir '^ho|a$'
    -->Child Table silinmeden Parent Table silinemez.
    -->Parant Table olmayan bir id'ye sahip datayı Child Table'a ekleyemeyiz
    -->Rename sadece isimleri değiştir modify tada tiplerini de değiştirir
    -->JDBC bir kütüphanedir. Javadaki objeleri fieldlar üzerinde sqldeki tablolara sütun kaydı olarak giriş çıkış yapar
    -->SQL önce genel konular girilir sonra özel konular girilir
    -->
    -->
    -->
    -->
    -->
    -->
    -->
    -->
    -->
    -->
    -->
    -->
    -->
    -->
    -->
    -->
    -->
    -->
    -->
    -->



     */
}
